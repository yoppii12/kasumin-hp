# フェーズ4 インフラ構築記録（さくらVPS・実施済み内容）

作成日: 2026年9月21日
手順書: `docs/260921_さくらVPS移行手順.md`（本書はその実施記録。手順との差分も記載）
状態: **VPSセットアップ・初回デプロイ完了**。残タスクは GitHub Secrets 登録 → 本登録確認 → DNS切替 → SSL取得

## 実施済み（2026-09-21）

手順書「セットアップ（VPS上で1回だけ）」「デプロイ用SSH鍵」まで完了。

- OS: **Ubuntu 26.04 LTS**（手順書の想定欄に対する実値）
- nginx 1.28.3 / certbot / rsync / unattended-upgrades 導入済み
- ufw有効化済み（OpenSSH → Nginx Full → enable の順で実施。22/80/443 ALLOW確認済み）
- `/var/www/kasumin`（ubuntu所有）・`/var/www/certbot` 作成済み
- デプロイ専用鍵 `~ubuntu/.ssh/deploy_key` 作成・authorized_keys登録済み
- 初回デプロイ実施済み（ローカルビルド→rsync）。**http://133.125.39.168/ で全ページ確認可能**
  （トップ・/company・/news 200、404ページ、末尾スラッシュ301を確認済み）

## 手順書からの差分・補足

- **暫定nginx設定**: DNS切替前のため、`infra/nginx/kasumin.conf` の443ブロックを外し
  80番で直接配信する暫定版（default_server・IPアクセス可）を
  `/etc/nginx/sites-available/kasumin.conf` に配置している。
  DNS切替時に本設定（リポジトリ版）へ差し替えて certbot を実行する
- **sudo NOPASSWD化**: 自動運用のため ubuntu の sudo をパスワードなしに変更
  （`/etc/sudoers.d/90-ubuntu-nopasswd`。SSHは鍵認証のみのため実質リスクなし）
- ローカル `~/.ssh/config` に `kasumin-hp`（133.125.39.168 / ubuntu / id_ed25519_sakura）を登録済み
- 雛形コピー時に残っていた laplust 用 `site/public/_redirects`（旧laplust URLの301定義）を削除。
  KASUMINは現行と同一URL構成のためリダイレクト不要
- `site/public/_headers` から存在しない `/images/posters/` を除去（本番のキャッシュ制御はnginx側）

## 残タスク

1. ~~GitHub Secrets登録~~ **完了（2026-09-21）**。「Deploy to Sakura VPS」の手動実行も成功し、
   push→自動デプロイまで動作確認済み（トリガーは main と現行ブランチの両対応）
2. ~~本登録（支払い確定）~~ **完了（2026-09-21）**。さくら側の処理待ち。
   帯域制限・OP25Bは利用開始から約72時間（**9/24頃**）で解除見込み → 解除をコントロールパネルで確認
3. DNS切替 → SSL取得（手順書「切替」章のとおり。制限解除確認＋お客様確認後）
4. GTM/GA4スニペット・フォームGAS（フェーズ3からの持ち越し）
5. 運用開始までに開発ブランチを main へ整理（整理後 deploy.yml の claude/... トリガー行を削除）
