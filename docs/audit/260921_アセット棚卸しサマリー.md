# アセット棚卸しサマリー

作成日：2026年9月21日
対象：kasumin.biz 全23ページのJS実行後DOM（PC 1440px＋SP 390px、スクロール済み・遅延読み込み込み）

## Studioアセット（storage.googleapis.com 配下）

- ユニークアセット数：**51**（DOM参照ベース）
- 拡張子/種別内訳：webp: 38, jpg: 5, (png): 4, woff2: 2, ttf: 2
- **動画（mp4）は0件**。LAplustで問題になった大容量動画は存在しない
- プロジェクトID別：bXqzew1ZOD: 42, production-os-assets: 8, NxqgdRVEa1: 1
  - `bXqzew1ZOD` がKASUMINサイト本体のStudioプロジェクトID
  - `NxqgdRVEa1` は**LAplustのプロジェクトID**。共通デザイン部品（アイコン等）がLAplust側バケットから参照されている＝「デザイン同一」の裏付け。移行時は自サイト配下にコピーして参照を切る
  - `production-os-assets` はSTUDIO新基盤の共通アセット（OGP画像・ファビコン・メンバー写真・FontAwesomeフォント）

## 退避（ダウンロード）結果

- `assets_backup/` に**70ファイル・約19.7MB**を退避済み（`_small`縮小版が参照されていたものは元解像度も併せて取得。失敗0件）
- `assets_backup/download_log.json` に URL→ファイル→サイズ→Content-Type の対応を記録
- 再構築時は `site/public/` 配下に必要分を配置し、参照URLを置換する（対応表はこのログとCSVが元データ）

## 抽出元

`<img src>` / `data-src` / `srcset` / `<video>` / `poster` / インラインstyleと`<style>`の background-image / og:image / favicon
